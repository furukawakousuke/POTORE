<div class="container">
  <div class="row">
    <div class="col-6 mt-3">
      <p class="lead">Reports</p>
    </div>
  </div>
  <div class="row">
    <div class="col-12">
      <table class="table">
        <thead>
          <tr>
            <th></th>
            <th>通報したUser</th>
            <th>通報されたUser</th>
            <th>投稿写真</th>
            <th>更新日時</th>
          </tr>
        </thead>
        <% @reports.each do |report| %>
        <tbody>
          <tr>
            <td><%= link_to admin_report_path(report.id),class:"btn btn-outline-secondary btn-sm" do %>
            <%= report.id %> <% end %>
            <td><%= link_to admin_poster_path(report.reporter),class:"btn btn-light btn-sm" do %>
            <%= report.reporter.user_name %><% end %>
            <td><%= link_to admin_poster_path(report.reported),class:"btn btn-light btn-sm" do %>
            <%= report.reported.user_name %><% end %>
            <td ><%= link_to admin_post_photo_path(report.post_photo) do %>
            <%= image_tag report.post_photo.get_image,:size=>"100",class:"img-fluid" %><% end %>
            <td><%= report.updated_at.strftime('%Y/%m/%d') %>
          </tr>
          <% end %>
        </tbody>
      </table>
      <%= paginate @reports %>
    </div>
  </div>
</div>