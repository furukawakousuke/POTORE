 <% if posters.exists? %>
  <% @favorite.each do |favorite| %>
    <%= link_to favorite_path(favorite.id),class:"text-dark text-decoration-none" do %>
       <%= image_tag favorite.get_image(100,100),:size => '80x80',class:"rounded-circle mr-2 mt-2 " %>
       <span class="mr-5 ml-3">
       <%= poster.user_name %>
       </span>
          
       <% end %>
   <% end %>
   <% else %>
     <p>ユーザーはいません</p>
  <% end %>